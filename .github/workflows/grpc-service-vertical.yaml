name: grpc-service-vertical
on:
  push:
    paths:
      - 'go/grpc-service-vertical/**'
jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Lint grpc-service-vertical
        uses: gardenbed/actions/go-lint@main
        with:
          path: ./go/grpc-service-vertical
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test grpc-service-vertical
        uses: gardenbed/actions/go-cover@main
        with:
          path: ./go/grpc-service-vertical
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Build grpc-service-vertical
        uses: gardenbed/actions/basil-build@main
        with:
          path: ./go/grpc-service-vertical
